bind=CTRL SHIFT, Y, exec, /usr/bin/morghulctl -q && /usr/bin/morghulis
bindr=Super, Super_L, exec, /usr/bin/morghulctl -t Runner
bindr=Super, I, exec, /usr/bin/morghulctl -i
# bindr=Super, X, exec, /usr/bin/morghulctl -t Powermenu

layerrule= animation slide, OnScreenDisplay
layerrule= animation popin, Runner
# layerrule= animation popin, NavBar
